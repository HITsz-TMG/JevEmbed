# Runtime dependencies for local models, HTTP backend and HTTP server.
# Use Python 3.10-3.12 for this tested model stack.
# Model-library versions are pinned in requirements-models-tested.txt.
-c requirements-models-tested.txt

PyYAML>=6,<7                  # YAML model configuration
numpy>=1.26,<2                # Sentence Transformers embedding arrays
torch>=2.1,<3                 # CPU/CUDA inference; choose a suitable PyTorch wheel
transformers==4.51.0          # KaLM custom code and Qwen3 compatibility
sentence-transformers==5.3.0  # Repository-provided model modules and pooling
huggingface-hub>=0.30,<1       # Download paired-projection checkpoints
httpx>=0.27,<1                # Optional /v1/embeddings backend
fastapi>=0.115,<1             # Optional Jev-shaped HTTP API
uvicorn>=0.30,<1              # HTTP server runner
